diff --git a/sglang_omni/models/minimax_music3/stages.py b/sglang_omni/models/minimax_music3/stages.py
--- a/sglang_omni/models/minimax_music3/stages.py
+++ b/sglang_omni/models/minimax_music3/stages.py
@@ -22,4 +22,12 @@ logger = logging.getLogger(__name__)
 _DEFAULT_AR_CONCURRENCY = int(os.environ.get("MINIMAX_MUSIC3_AR_CONCURRENCY", "16"))
 
 
+def _resolve_ar_dtype(device: str) -> str:
+    return (
+        "bfloat16"
+        if torch.cuda.get_device_capability(torch.device(device))[0] >= 8
+        else "float16"
+    )
+
+
 def create_preprocessing_executor(model_path: str, **_: Any) -> SimpleScheduler:
@@ -64,7 +72,7 @@ def create_ar_executor(
         model_path,
         device=device,
         gpu_id=gpu_id,
-        dtype="bfloat16",
+        dtype=_resolve_ar_dtype(device),
         server_args_overrides=overrides or None,
     )
     logger.info(
