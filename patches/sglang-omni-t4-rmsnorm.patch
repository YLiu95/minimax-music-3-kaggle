diff --git a/sglang_omni/vendor/sglang/layers.py b/sglang_omni/vendor/sglang/layers.py
--- a/sglang_omni/vendor/sglang/layers.py
+++ b/sglang_omni/vendor/sglang/layers.py
@@ -70,6 +70,13 @@ def _patched_forward_cuda(
                 residual = residual + post_residual_addition
             return x, residual
         return x
+    if x.is_cuda and torch.cuda.get_device_capability(x.device)[0] < 8:
+        return self.forward_native(
+            x,
+            residual,
+            post_residual_addition=post_residual_addition,
+            **kwargs,
+        )
     if residual is not None and residual.dtype != x.dtype:
         return self.forward_native(
             x,
